#include "cab202_graphics.h"

void draw_paddles( void ) {
	screen_width(600);
	screen_height(400);

	draw_line(screen_width() -1, '*' )
	draw_line(screen_width() +1, '*' )
	draw_line(screen_width() -1, '*' )
	y = (h-p) / 2

	show_screen( );
}